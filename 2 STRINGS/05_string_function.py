name = "jonathan is a good boy"

print(len(name))                    # finds the length of a string

print(name.endswith("than"))        # check the string if it ends with "than or anything in brackets" 
                                        # returns true oterwise false

print(name.startswith("jona"))      # check the string if it stars with "jona or anything in brackets" 
                                        # returns true oterwise false

print(name.capitalize())            # capatalize the first character of string 

print(name.replace("good", "bad" )) # replace any thing you want in a string
